[package]
name = "wh-proto"
version = "0.1.1-alpha"
edition.workspace = true
# Not license.workspace: this crate, alone among the four, contains a Rust port of MIT-licensed
# Sparklink source (frame.rs, cmds.rs; see their own module docs and LICENSE section 1), so the
# workspace-wide "LicenseRef-Proprietary" alone understates it. See THIRD_PARTY_NOTICES.md.
license = "LicenseRef-Proprietary AND MIT"
publish = false

[dependencies]
thiserror.workspace = true

[dev-dependencies]
serde_json.workspace = true
